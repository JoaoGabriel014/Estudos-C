#include <stdio.h>
#include <string.h> 
struct Funcionario{
    char nome[50];
    float salario;
};

int main(){
    struct Funcionario funcionarios [4];

    for (int i = 0; i < 4; i++) {
        printf("Digite o nome do funcionario %d: ", i + 1);
        scanf("%49s", ))
    }
}